# configs

- `build_config.json`
  - Embedding / chunking / shard construction settings used by the production builder.
- `faiss_candidates.json`
  - Candidate FAISS configurations used only by `research/benchmarking`.
- `faiss_config.json`
  - Selected FAISS configuration used by `build_production.py`.
